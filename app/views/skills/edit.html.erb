<div id="sheet_header">
<span class="right"><%= link_to  h(@skill), @skill %> </span>
<h2>Editing <%=h @skill.fancy_name %></h2>

</div>
<div id="sheet">
<%= semantic_form_for(@skill) do |f| %>
	<%= render 'form', :f => f %>

<% end %>

<%= link_to 'Back', skills_path %>

</div>