<h1>Editing group</h1>

<%= semantic_form_for(@group) do |f| %>
	<%= render 'form', :f => f %>
<% end %>

<%= link_to 'View Group', @group %> |
<%= link_to 'Destroy Group', @group, :confirm => 'Are you sure?', :method => :delete %>